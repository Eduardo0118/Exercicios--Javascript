var myvar;

myvar = 10;

var soma = 15 + 8;

soma += 1;

soma *= 3;

var souninja = true;

var comida = ["arroz", "feijão", "ovo"];

comida[1];

soma == myvar;
soma === myvar;

myvar <= soma;

function divisao(x, y) {
  return x / y;
}

divisao(10, 2);